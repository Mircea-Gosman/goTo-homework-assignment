// export const handleDateSelection = (selectedDate: Date) => {
//     fireTaskUpdate({ _id: task._id, endDate: selectedDate })
// }

// export const handleStatusChange = (newStatus: Status) => {
//     fireTaskUpdate({ _id: task._id, status: newStatus })
// }

// export const handleDescriptionChange = (newDescription: string) => {
//     throttleTaskUpdate({ _id: task._id, description: newDescription})
// }

// export const handleTitleChange = (newTitle: string) => {
//     throttleTaskUpdate({ _id: task._id, title: newTitle})
// }

export {}